version: '3.8'

services:
  db:
    image: postgres:13.4-alpine
    environment:
      POSTGRES_PASSWORD: password
    volumes:
      - ./.db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    tty: true
